import { compressImage, compressImageToSize } from '../imageCompression';

// Note: imageCompression.ts uses HTML Canvas API which is browser-specific.
// This is a React Native project, so these tests are skipped.
// The module should be replaced with a React Native-compatible image compression solution.

describe.skip('imageCompression', () => {
  it('should be replaced with React Native-compatible image compression', () => {
    // This test is a placeholder to indicate that imageCompression.ts
    // uses browser APIs (Image, canvas) that don't work in React Native.
    // Consider using expo-image-manipulator or similar for React Native.
  });
});
